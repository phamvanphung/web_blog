# Database
DATABASE_URL="mysql://9ent:CHANGE_ME@localhost:3306/9ent_blog"

# App
NODE_ENV="development"
APP_URL="http://localhost:3000"
SESSION_COOKIE_NAME="sid"
SESSION_TTL_DAYS="14"

# Storage — relative for local dev (Windows + Linux both OK)
# In production (VPS Linux), override UPLOAD_ROOT in real .env to /srv/9ent/storage/uploads
UPLOAD_ROOT="./storage/uploads"
UPLOAD_PUBLIC_BASE="/uploads"

# Logging
LOG_LEVEL="info"
