// components/editor/slashCommand.ts
// Stub — full implementation lands in Task 5.
export function renderSlashMenu(): { onStart: () => void; onUpdate: () => void; onKeyDown: () => boolean; onExit: () => void } {
  return { onStart: () => {}, onUpdate: () => {}, onKeyDown: () => false, onExit: () => {} };
}
